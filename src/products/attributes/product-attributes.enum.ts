export enum Type {
    IMPORT = 'import',
    CUSTOM = 'custom'
}

export enum TypeUI {
    Range = 'range',
    Checkbox = 'checkbox'
}

export enum TypeSource {
    HEADDER = 'headder',
    ATTRIBUTES = 'attributes'
}

export enum SectionViews {
    MAIN = 'main',
    ATTRIBUTES = 'attributes',
    PREVIEW = 'preview'
}